# Product photos

This folder contains the product photos used by the 91 published products.
They are used with the partner store's permission and are not licensed for reuse.

`dbo.SB_ProductMetadata.ImagePath` stores a relative filename. The API serves
this directory read-only at `PRODUCT_IMAGE_PUBLIC_PATH` (default
`/product-images`).
